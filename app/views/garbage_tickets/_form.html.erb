<%= form_for @garbage_ticket do |f| %>
  <% if @garbage_ticket.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@garbage_ticket.errors.count, "error") %> prohibited this garbage_ticket from being saved:</h2>

      <ul>
      <% @garbage_ticket.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <div class="input-group">
      <%= f.text_field :location, class: "form-control span6", placeholder: "Enter Location" %>
    </div>
  </div>

  <div class="field">
  </div>

  <div class="field">
    <%= f.telephone_field :mobile, placeholder: "Mobile" %>
  </div>

  <div class="field">
    <%= f.email_field :email, placeholder: "Email" %>
  </div>
  <%= f.hidden_field :level %>
  <%= f.hidden_field :latitude %>
  <%= f.hidden_field :longitude %>
  <%= f.hidden_field :is_open %>
  <div class="actions">
    <%= f.submit "Add", class: "btn btn-lg btn-success" %>
  </div>
<% end %>
